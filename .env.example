# 服务端口
PORT=34567

# 文件上传大小限制（支持 10mb, 50mb, 1gb 等格式，默认 10mb）
# 限制单次请求的文件大小，分片上传自动放宽至 chunk 大小 + 1MB
UPLOAD_LIMIT=10mb

# 环境类型（development / production）
NODE_ENV=development

# 前端开发服务器端口（仅开发模式）
VITE_CLIENT_PORT=5175
